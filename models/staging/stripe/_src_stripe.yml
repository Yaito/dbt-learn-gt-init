sources:
    - name: stripe
      database: raw
      schema: stripe
      tables:
        - name: payment
          config:
            freshness:
              warn_after: {count: 72, period: hour}
              error_after: {count: 168, period: hour}
            loaded_at_field: convert_timezone('EST',_batched_at)
              